#include "vector.hpp"
#include <iterator>
#include <vector>
using namespace std;
int main()
{
	ft::vector<int> a(5, 4);
	// vector<int> v1;
	//  v1.assign (7,100);  
    // Declaring an iterator
    // vector<int>::iterator i1;
	// ft::vector<int> a(v1.begin(), v1.end());
	// a.say();
	// container::vector<int> a();

}